import './App.css';

// This component is no longer used directly since we're using React Router
// The main application is now rendered through the router in main.tsx
function App() {
  return null;
}

export default App;
